#!/usr/bin/env python3
"""converts a float to its string representation."""


def to_str(n: float) -> str:
    """args: n (float): the input float.
    returns: str: the string representation of the float."""
    return str(n)
