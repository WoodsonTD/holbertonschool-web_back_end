#!/usr/bin/env python3
"""concatenates two strings and returns the result as a string"""


def concat(str1: str, str2: str) -> str:

    """args: str1 (str): the first string. str2 (str): the second string.
    returns: str the concatenation of 'str1' and 'str2' as a string."""
    return str1 + str2
